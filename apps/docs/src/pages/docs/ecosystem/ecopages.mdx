import { CodeTabs } from '@/components/code-tabs';
import { DocsLayout } from '@/layouts/docs-layout';
import { Banner } from '@/components/banner/banner';

export const config = {
  layout: DocsLayout,
};

export const getMetadata = () => ({
  title: 'Docs | ecopages CLI',
  description: 'Use the official ecopages CLI to scaffold and run Ecopages projects',
});

# ecopages CLI

The `ecopages` npm package is the official and preferred command-line tool for scaffolding and running Ecopages projects.

It is best when you want:

- **Fast project creation** from templates
- **Consistent app lifecycle commands** (`dev`, `build`, `preview`, `start`)
- **A single entrypoint** for common local workflows

<Banner type="warning">
  <Banner.Title>Draft / Experimental</Banner.Title>
  <p>The CLI is currently evolving. Keep versions pinned in team projects to ensure stable team workflows.</p>
</Banner>

## Install

You can run the CLI directly with `bunx` (no global install required):

<CodeTabs
	name="ecopages-cli-run"
	tabs={[
		{ id: 'pnpm', label: 'pnpm', code: 'pnpm dlx ecopages --help' },
		{ id: 'npm', label: 'npm', code: 'npx ecopages --help' },
		{ id: 'bun', label: 'bun', code: 'bunx ecopages --help' },
	]}
	defaultSelectedKey="pnpm"
/>

Or install it in a project:

<CodeTabs
	name="ecopages-cli-install"
	tabs={[
		{ id: 'pnpm', label: 'pnpm', code: 'pnpm add ecopages' },
		{ id: 'npm', label: 'npm', code: 'npm install ecopages' },
		{ id: 'bun', label: 'bun', code: 'bun add ecopages' },
	]}
	defaultSelectedKey="pnpm"
/>

## Initialize Projects

### Init Command

Use the init command with a target directory, then optionally select a template:

<CodeTabs
	name="ecopages-init-command"
	tabs={[
		{ id: 'pnpm', label: 'pnpm', code: 'pnpm dlx ecopages init my-app' },
		{ id: 'npm', label: 'npm', code: 'npx ecopages init my-app' },
		{ id: 'bun', label: 'bun', code: 'bunx ecopages init my-app' },
	]}
	defaultSelectedKey="pnpm"
/>

### One-Shot Commands (Complete)

Use these when you want a full bootstrap in one command chain.

**Basic starter** - Creates a minimal Ecopages app with default setup. Example: <a href="https://github.com/ecopages/ecopages/tree/main/examples/starter-jsx" target="_blank">starter-jsx</a>.

<CodeTabs
	name="ecopages-basic-starter"
	tabs={[
		{ id: 'pnpm', label: 'pnpm', code: 'pnpm dlx ecopages init ecopages-app && cd ecopages-app && pnpm install && pnpm run dev' },
		{ id: 'npm', label: 'npm', code: 'npx ecopages init ecopages-app && cd ecopages-app && npm install && npm run dev' },
		{ id: 'bun', label: 'bun', code: 'bunx ecopages init ecopages-app && cd ecopages-app && bun install && bun dev' },
	]}
	defaultSelectedKey="pnpm"
/>

**Blog with KitaJS** - Creates a content-focused blog using the `blog-jsx` template. Example: <a href="https://github.com/ecopages/ecopages/tree/main/examples/blog-jsx" target="_blank">blog-jsx</a>.

<CodeTabs
	name="ecopages-blog-jsx"
	tabs={[
		{ id: 'pnpm', label: 'pnpm', code: 'pnpm dlx ecopages init blog-jsx --template blog-jsx && cd blog-jsx && pnpm install && pnpm run dev' },
		{ id: 'npm', label: 'npm', code: 'npx ecopages init blog-jsx --template blog-jsx && cd blog-jsx && npm install && npm run dev' },
		{ id: 'bun', label: 'bun', code: 'bunx ecopages init blog-jsx --template blog-jsx && cd blog-jsx && bun install && bun dev' },
	]}
	defaultSelectedKey="pnpm"
/>

**Blog with React** - Creates a React-powered blog with routing preconfigured. Example: <a href="https://github.com/ecopages/ecopages/tree/main/examples/blog-react" target="_blank">blog-react</a>.

<CodeTabs
	name="ecopages-blog-react"
	tabs={[
		{ id: 'pnpm', label: 'pnpm', code: 'pnpm dlx ecopages init blog-react --template blog-react && cd blog-react && pnpm install && pnpm run dev' },
		{ id: 'npm', label: 'npm', code: 'npx ecopages init blog-react --template blog-react && cd blog-react && npm install && npm run dev' },
		{ id: 'bun', label: 'bun', code: 'bunx ecopages init blog-react --template blog-react && cd blog-react && bun install && bun dev' },
	]}
	defaultSelectedKey="pnpm"
/>

**React app with Better Auth + Drizzle** - Creates a full app starter with auth and database workflow included. Example: <a href="https://github.com/ecopages/ecopages/tree/main/examples/with-react-better-auth" target="_blank">with-react-better-auth</a>.

<CodeTabs
	name="ecopages-better-auth"
	tabs={[
		{ id: 'pnpm', label: 'pnpm', code: 'pnpm dlx ecopages init with-react-better-auth --template with-react-better-auth && cd with-react-better-auth && pnpm install && pnpm run db:generate && pnpm run db:migrate && pnpm run dev' },
		{ id: 'npm', label: 'npm', code: 'npx ecopages init with-react-better-auth --template with-react-better-auth && cd with-react-better-auth && npm install && npm run db:generate && npm run db:migrate && npm run dev' },
		{ id: 'bun', label: 'bun', code: 'bunx ecopages init with-react-better-auth --template with-react-better-auth && cd with-react-better-auth && bun install && bun db:generate && bun db:migrate && bun dev' },
	]}
	defaultSelectedKey="pnpm"
/>

### Step-by-Step (Individual)

#### 1) Basic starter

<CodeTabs
	name="ecopages-basic-starter-steps"
	tabs={[
		{ id: 'pnpm', label: 'pnpm', code: `pnpm dlx ecopages init ecopages-app
cd ecopages-app
pnpm install
pnpm run dev` },
		{ id: 'npm', label: 'npm', code: `npx ecopages init ecopages-app
cd ecopages-app
npm install
npm run dev` },
		{ id: 'bun', label: 'bun', code: `bunx ecopages init ecopages-app
cd ecopages-app
bun install
bun dev` },
	]}
	defaultSelectedKey="pnpm"
/>

#### 2) Blog with KitaJS

<CodeTabs
	name="ecopages-blog-jsx-steps"
	tabs={[
		{ id: 'pnpm', label: 'pnpm', code: `pnpm dlx ecopages init blog-jsx --template blog-jsx
cd blog-jsx
pnpm install
pnpm run dev` },
		{ id: 'npm', label: 'npm', code: `npx ecopages init blog-jsx --template blog-jsx
cd blog-jsx
npm install
npm run dev` },
		{ id: 'bun', label: 'bun', code: `bunx ecopages init blog-jsx --template blog-jsx
cd blog-jsx
bun install
bun dev` },
	]}
	defaultSelectedKey="pnpm"
/>

#### 3) Blog with React

<CodeTabs
	name="ecopages-blog-react-steps"
	tabs={[
		{ id: 'pnpm', label: 'pnpm', code: `pnpm dlx ecopages init blog-react --template blog-react
cd blog-react
pnpm install
pnpm run dev` },
		{ id: 'npm', label: 'npm', code: `npx ecopages init blog-react --template blog-react
cd blog-react
npm install
npm run dev` },
		{ id: 'bun', label: 'bun', code: `bunx ecopages init blog-react --template blog-react
cd blog-react
bun install
bun dev` },
	]}
	defaultSelectedKey="pnpm"
/>

#### 4) React app with Better Auth + Drizzle

<CodeTabs
	name="ecopages-better-auth-steps"
	tabs={[
		{ id: 'pnpm', label: 'pnpm', code: `pnpm dlx ecopages init with-react-better-auth --template with-react-better-auth
cd with-react-better-auth
pnpm install
pnpm run db:generate
pnpm run db:migrate
pnpm run dev` },
		{ id: 'npm', label: 'npm', code: `npx ecopages init with-react-better-auth --template with-react-better-auth
cd with-react-better-auth
npm install
npm run db:generate
npm run db:migrate
npm run dev` },
		{ id: 'bun', label: 'bun', code: `bunx ecopages init with-react-better-auth --template with-react-better-auth
cd with-react-better-auth
bun install
bun db:generate
bun db:migrate
bun dev` },
	]}
	defaultSelectedKey="pnpm"
/>

## Runtime Notes

The CLI auto-detects Bun when launched from Bun tooling and otherwise defaults to the Node thin-host path. You can override that explicitly with `--runtime bun`, `--runtime node`, or `--runtime node-experimental` on the lifecycle commands.

## Command Details

This page focuses on setup and project bootstrapping.

For the complete command, flag, and environment variable reference, see [CLI Reference](/docs/reference/cli-reference).

## When To Use `ecopages` vs `@ecopages/*` Packages

- Use **`ecopages` CLI** for scaffolding and local app lifecycle commands.
- Use **`@ecopages/*` packages** from npm for framework APIs in app code.

For the complete package map, see [Ecosystem Packages](/docs/ecosystem/packages).
