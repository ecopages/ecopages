import { CodeSnippetTabs, CodeTab } from '@/components/code-snippet-tabs/code-snippet-tabs.kita';
import { DocsLayout } from '@/layouts/docs-layout';
import { Banner } from '@/components/banner/banner.kita';

export const config = {
  layout: DocsLayout,
};

export const getMetadata = () => ({
  title: 'Docs | ecopages CLI',
  description: 'Use the official npm ecopages CLI to scaffold and run Ecopages projects with Bun',
});

# ecopages CLI

The `ecopages` npm package is the official and preferred command-line tool for scaffolding and running Ecopages projects.

It is best when you want:

- **Fast project creation** from templates
- **Consistent Bun commands** (`dev`, `build`, `preview`, `start`)
- **A single entrypoint** for common local workflows

<Banner type="warning">
  <Banner.Title>Draft / Experimental</Banner.Title>
  <p>The CLI is currently evolving. Keep versions pinned in team projects to ensure stable team workflows.</p>
</Banner>

## Install

You can run the CLI directly with `bunx` (no global install required):

<CodeSnippetTabs>
  <CodeTab name="pnpm">
```bash
pnpm dlx ecopages --help
```
  </CodeTab>
  <CodeTab name="npm">
```bash
npx ecopages --help
```
  </CodeTab>
  <CodeTab name="bun">
```bash
bunx ecopages --help
```
  </CodeTab>
</CodeSnippetTabs>

Or install it in a project:

<CodeSnippetTabs>
  <CodeTab name="pnpm">
```bash
pnpm add ecopages
```
  </CodeTab>
  <CodeTab name="npm">
```bash
npm install ecopages
```
  </CodeTab>
  <CodeTab name="bun">
```bash
bun add ecopages
```
  </CodeTab>
</CodeSnippetTabs>

## Initialize Projects

### Interactive Init

Use the base init command when you want to pick options interactively:

<CodeSnippetTabs>
  <CodeTab name="pnpm">
```bash
pnpm dlx ecopages init
```
  </CodeTab>
  <CodeTab name="npm">
```bash
npx ecopages init
```
  </CodeTab>
  <CodeTab name="bun">
```bash
bunx ecopages init
```
  </CodeTab>
</CodeSnippetTabs>

### One-Shot Commands (Complete)

Use these when you want a full bootstrap in one command chain.

**Basic starter** - Creates a minimal Ecopages app with default setup. Example: <a href="https://github.com/ecopages/ecopages/tree/main/examples/starter-jsx" target="_blank">starter-jsx</a>.

<CodeSnippetTabs>
  <CodeTab name="pnpm">
```bash
pnpm dlx ecopages init ecopages-app && cd ecopages-app && bun install && bun dev
```
  </CodeTab>
  <CodeTab name="npm">
```bash
npx ecopages init ecopages-app && cd ecopages-app && bun install && bun dev
```
  </CodeTab>
  <CodeTab name="bun">
```bash
bunx ecopages init ecopages-app && cd ecopages-app && bun install && bun dev
```
  </CodeTab>
</CodeSnippetTabs>

**Blog with KitaJS** - Creates a content-focused blog using the `blog-jsx` template. Example: <a href="https://github.com/ecopages/ecopages/tree/main/examples/blog-jsx" target="_blank">blog-jsx</a>.

<CodeSnippetTabs>
  <CodeTab name="pnpm">
```bash
pnpm dlx ecopages init blog-jsx --template blog-jsx && cd blog-jsx && bun install && bun dev
```
  </CodeTab>
  <CodeTab name="npm">
```bash
npx ecopages init blog-jsx --template blog-jsx && cd blog-jsx && bun install && bun dev
```
  </CodeTab>
  <CodeTab name="bun">
```bash
bunx ecopages init blog-jsx --template blog-jsx && cd blog-jsx && bun install && bun dev
```
  </CodeTab>
</CodeSnippetTabs>

**Blog with React** - Creates a React-powered blog with routing preconfigured. Example: <a href="https://github.com/ecopages/ecopages/tree/main/examples/blog-react" target="_blank">blog-react</a>.

<CodeSnippetTabs>
  <CodeTab name="pnpm">
```bash
pnpm dlx ecopages init blog-react --template blog-react && cd blog-react && bun install && bun dev
```
  </CodeTab>
  <CodeTab name="npm">
```bash
npx ecopages init blog-react --template blog-react && cd blog-react && bun install && bun dev
```
  </CodeTab>
  <CodeTab name="bun">
```bash
bunx ecopages init blog-react --template blog-react && cd blog-react && bun install && bun dev
```
  </CodeTab>
</CodeSnippetTabs>

**React app with Better Auth + Drizzle** - Creates a full app starter with auth and database workflow included. Example: <a href="https://github.com/ecopages/ecopages/tree/main/examples/with-react-better-auth" target="_blank">with-react-better-auth</a>.

<CodeSnippetTabs>
  <CodeTab name="pnpm">
```bash
pnpm dlx ecopages init with-react-better-auth --template with-react-better-auth && cd with-react-better-auth && bun install && bun db:generate && bun db:migrate && bun dev
```
  </CodeTab>
  <CodeTab name="npm">
```bash
npx ecopages init with-react-better-auth --template with-react-better-auth && cd with-react-better-auth && bun install && bun db:generate && bun db:migrate && bun dev
```
  </CodeTab>
  <CodeTab name="bun">
```bash
bunx ecopages init with-react-better-auth --template with-react-better-auth && cd with-react-better-auth && bun install && bun db:generate && bun db:migrate && bun dev
```
  </CodeTab>
</CodeSnippetTabs>

### Step-by-Step (Individual)

#### 1) Basic starter

<CodeSnippetTabs>
  <CodeTab name="pnpm">
```bash
pnpm dlx ecopages init ecopages-app
cd ecopages-app
pnpm install
pnpm run dev
```
  </CodeTab>
  <CodeTab name="npm">
```bash
npx ecopages init ecopages-app
cd ecopages-app
npm install
npm run dev
```
  </CodeTab>
  <CodeTab name="bun">
```bash
bunx ecopages init ecopages-app
cd ecopages-app
bun install
bun dev
```
  </CodeTab>
</CodeSnippetTabs>

#### 2) Blog with KitaJS

<CodeSnippetTabs>
  <CodeTab name="pnpm">
```bash
pnpm dlx ecopages init blog-jsx --template blog-jsx
cd blog-jsx
pnpm install
pnpm run dev
```
  </CodeTab>
  <CodeTab name="npm">
```bash
npx ecopages init blog-jsx --template blog-jsx
cd blog-jsx
npm install
npm run dev
```
  </CodeTab>
  <CodeTab name="bun">
```bash
bunx ecopages init blog-jsx --template blog-jsx
cd blog-jsx
bun install
bun dev
```
  </CodeTab>
</CodeSnippetTabs>

#### 3) Blog with React

<CodeSnippetTabs>
  <CodeTab name="pnpm">
```bash
pnpm dlx ecopages init blog-react --template blog-react
cd blog-react
pnpm install
pnpm run dev
```
  </CodeTab>
  <CodeTab name="npm">
```bash
npx ecopages init blog-react --template blog-react
cd blog-react
npm install
npm run dev
```
  </CodeTab>
  <CodeTab name="bun">
```bash
bunx ecopages init blog-react --template blog-react
cd blog-react
bun install
bun dev
```
  </CodeTab>
</CodeSnippetTabs>

#### 4) React app with Better Auth + Drizzle

<CodeSnippetTabs>
  <CodeTab name="pnpm">
```bash
pnpm dlx ecopages init with-react-better-auth --template with-react-better-auth
cd with-react-better-auth
pnpm install
pnpm run db:generate
pnpm run db:migrate
pnpm run dev
```
  </CodeTab>
  <CodeTab name="npm">
```bash
npx ecopages init with-react-better-auth --template with-react-better-auth
cd with-react-better-auth
npm install
npm run db:generate
npm run db:migrate
npm run dev
```
  </CodeTab>
  <CodeTab name="bun">
```bash
bunx ecopages init with-react-better-auth --template with-react-better-auth
cd with-react-better-auth
bun install
bun db:generate
bun db:migrate
bun dev
```
  </CodeTab>
</CodeSnippetTabs>

## Command Details

This page focuses on setup and project bootstrapping.

For the complete command, flag, and environment variable reference, see [CLI Reference](/docs/reference/cli-reference).

## When To Use `ecopages` vs JSR Packages

- Use **`ecopages` CLI** for scaffolding and local app lifecycle commands.
- Use **`@ecopages/*` packages** from JSR for framework APIs in app code.

For the complete package map, see [Ecosystem Packages](/docs/ecosystem/packages).
