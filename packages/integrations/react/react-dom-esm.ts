import * as ReactDOM from 'react-dom';

export const {
  createPortal,
  flushSync,
  useFormStatus,
  useFormState,
  prefetchDNS,
  preload,
  preloadModule,
  preinit,
  preinitModule,
  requestFormReset,
} = ReactDOM;

export default ReactDOM;
