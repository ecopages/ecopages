import { BaseLayout } from '../layouts/base-layout/base-layout.kita';

export const config = {
	layout: BaseLayout,
	dependencies: {
		stylesheets: ['./docs.css'],
	},
};

<div class="docs-page__prose">

# MDX Route

This page is intentionally part of the kitchen sink so the app exercises MDX beside JSX pages, explicit routes, grouped handlers, and request middleware.

- File-system pages still own the main route tree.
- Explicit routes layer in extra pages and APIs from `app.ts`.
- The HTML shell and route layout are shared across both models.
- Request middleware populates `locals` for dynamic pages and handlers.

```ts
export const config = {
	layout: BaseLayout,
};
```

Try these routes next:

- [Middleware locals](/patterns/middleware?flag=mdx&flag=runtime)
- [Catalog dynamic route](/catalog/semantic-html)
- [Explicit team page](/explicit/team)
- [API lab](/api-lab)

Return to the [home page](/).

</div>
