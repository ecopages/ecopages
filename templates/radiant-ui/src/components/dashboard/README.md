# Dashboard

`/dashboard` is an application shell: `AppLayout` plus `SidebarLayout`.

- `data-table/` — inventory table from the Radiant UI data-table story, with an
  in-memory store instead of MSW.
