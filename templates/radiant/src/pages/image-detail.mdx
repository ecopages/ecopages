import { BaseLayout } from '../layouts/base-layout';
import { EcoImage } from '@ecopages/image-processor/component/jsx';
import { logoOnGhDarkPng } from 'ecopages:images';

export const config = {
    layout: { component: BaseLayout, props: () => ({ prose: true }) },
};

# Image detail

The same image now uses the shared `data-view-transition="kita-image"` name, so the browser morphs it between pages.

<div data-view-transition="kita-image" data-view-transition-animate="fade" data-view-transition-duration="600ms" class="inline-block">
    <EcoImage {...logoOnGhDarkPng} alt="Kita-kamakura" width={800} height={600} class="rounded" />
</div>

[← Back to the image page](/image) · [Home](/)
