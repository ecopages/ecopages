import { BaseLayout } from '@/layouts/base-layout';

export const config = {
	layout: { component: BaseLayout, props: () => ({ prose: true }) },
};

# Content without ceremony

This page is rendered from **MDX** and can live alongside the integration's page and component files.

## Features

- Easy to use
- Fast
- Flexible
