import { BaseLayout } from '../layouts/base-layout';
import { EcoImage } from '@ecopages/image-processor/component/react';
import { logoOnGhDarkPng } from 'ecopages:images';

export const config = {
    layout: { component: BaseLayout, props: () => ({ prose: true }) },
};

# Images

The `ecopages/image-processor` plugin processes images at build time and generates responsive, optimized markup.

Click the image below to open the detail page and see a cross-page view transition.

<a href="/image-detail" className="image-link block text-center">
  <div data-view-transition="kita-image" data-view-transition-animate="fade" data-view-transition-duration="600ms" className="inline-block">
    <EcoImage {...logoOnGhDarkPng} alt="Kita-kamakura" width={320} height={240} className="rounded" />
  </div>
</a>

Check out the <a href="/" data-eco-reload>Home Page</a> to go back.
